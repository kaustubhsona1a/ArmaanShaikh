

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Configure your environment variables in `.env.local` (VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY)
3. Run the app:
   `npm run dev`
